@{
    ViewData["Title"] = "Mi Escritorio";
}

<div class="component-header">
    <h1><i class="fas fa-chart-bar"></i> Mi Escritorio</h1>
</div>


<style>
    .component-header {
        margin-bottom: 30px;
        padding-bottom: 20px;
        border-bottom: 2px solid var(--accent-color);
    }

    .component-header h1 {
        color: var(--text-primary);
        font-size: 20px;
        font-weight: 700;
        display: flex;
        align-items: center;
        gap: 15px;
    }

    .component-header i {
        color: var(--accent-color);
        font-size: 36px;
    }
</style>
